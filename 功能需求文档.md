# 校园失物招领智能匹配与 AI 图像识别系统 —— 功能需求文档

> 版本：v1.0　|　编制日期：2026-08-19
> 技术栈：SpringBoot 3.5 + Vue3 + MySQL 8.0 + Python FastAPI + MobileNetV3

---

## 1. 引言

### 1.1 编写目的

本文档对"校园失物招领智能匹配与 AI 图像识别系统"的功能需求进行完整、规范化的描述，明确系统的功能边界、业务规则、用户角色与接口约定，作为系统设计、开发、测试与验收的依据。

### 1.2 项目背景

校园内失物与招领信息分散于公告栏、社交群聊等渠道，存在信息不集中、人工比对效率低、失主与拾主难以快速建立联系等问题。本系统面向校园师生，提供失物/招领信息的统一发布、检索、智能匹配与认领闭环服务，并引入 AI 图像识别能力，实现"以图匹配、以图识物"的智能化失物招领。

### 1.3 术语定义

| 术语 | 说明 |
|------|------|
| 失物帖（LOST） | 用户发布的"我丢了东西"信息 |
| 招领帖（FOUND） | 用户发布的"我捡到东西"信息 |
| 匹配记录 | 一条失物帖与一条招领帖之间自动计算出的匹配关系 |
| 认领 | 失主针对某条匹配记录向拾主发起的取回物品申请 |
| 特征向量 | 由 MobileNetV3 从图片提取的 960 维浮点数向量，用于计算图像相似度 |
| AI 降级 | AI 服务不可用时，系统自动退回纯文字匹配，保证核心功能可用 |

---

## 2. 系统概述

### 2.1 系统目标

1. 提供失物/招领信息的**发布、检索、管理**能力；
2. 通过**多维度加权算法**自动匹配失物与招领信息，减少人工比对；
3. 集成 **AI 图像识别**，实现图片自动分类与图像相似度匹配；
4. 形成**发布 → 匹配 → 通知 → 认领 → 归档**的完整业务闭环；
5. 通过**数据看板**可视化展示系统运行数据；
6. 在 AI 服务故障时**自动降级**，保障核心功能稳定可用。

### 2.2 系统角色

| 角色 | 标识 | 权限说明 |
|------|------|---------|
| 游客 | 未登录 | 浏览信息广场、查看帖子详情、注册、登录 |
| 普通用户 | USER | 游客权限 + 发布/编辑/删除/归档帖子、申请认领、处理认领、查看匹配、接收通知 |
| 管理员 | ADMIN | 普通用户全部权限 + 查看数据看板、帖子管理（查看全部/下架/删除）、用户管理 |

### 2.3 技术架构

```
┌──────────────────────────────────────┐
│        前端 Vue3 + Element Plus       │
│   信息广场 │ 发布 │ 匹配 │ 认领 │ 看板  │
└──────────────────┬───────────────────┘
                   │ HTTP /api（Vite 代理）
┌──────────────────▼───────────────────┐
│       后端 SpringBoot 3.5 (8080)      │
│  用户 │ 帖子 │ 匹配 │ 认领 │ 通知SSE    │
│  统计 │ JWT鉴权 │ 上传 │ AI客户端       │
└───────┬───────────────────┬──────────┘
        │                   │ HTTP（特征提取/分类）
┌───────▼────────┐  ┌───────▼──────────┐
│   MySQL (3306)  │  │ Python FastAPI   │
│    业务数据     │  │  AI 服务 (8000)   │
└────────────────┘  │  MobileNetV3     │
                    └──────────────────┘
```

### 2.4 运行环境

| 组件 | 版本要求 |
|------|---------|
| JDK | 1.8+ |
| Maven | 3.6+ |
| Node.js | 16+ |
| MySQL | 8.0 |
| Python | 3.9+（含 PyTorch、torchvision） |

---

## 3. 功能需求

> 功能编号规则：FR-模块序号.功能序号。优先级：P0（核心）/ P1（重要）/ P2（增强）。

---

### 3.1 用户认证模块

#### FR-1.1 用户注册（P0）

- **功能描述**：游客提交账号信息注册成为系统用户。
- **输入**：用户名（唯一）、密码、昵称、手机号（可选）。
- **业务规则**：
  1. 用户名不得重复，重复时返回错误提示；
  2. 密码采用 BCrypt 加密存储，明文不落库、不外传；
  3. 昵称、手机号为可选项。
- **输出**：注册成功提示。

#### FR-1.2 用户登录（P0）

- **功能描述**：用户使用用户名 + 密码登录，获取访问凭证。
- **输入**：用户名、密码。
- **业务规则**：
  1. 密码校验采用 BCrypt 比对；
  2. 登录成功后签发 JWT Token（有效期 168 小时，可配置）；
  3. 返回的用户信息中不包含密码字段。
- **输出**：JWT Token 与用户基本信息（id、username、nickname、role 等）。

#### FR-1.3 获取当前用户信息（P0）

- **功能描述**：根据 Token 返回当前登录用户信息，用于前端恢复登录态。
- **输入**：请求头 `Authorization: Bearer <token>`。
- **输出**：当前用户信息（不含密码）。

#### FR-1.4 身份认证与鉴权（P0）

- **功能描述**：除公开接口外，其余接口均需携带有效 JWT Token，由拦截器统一校验。
- **公开接口（免登录）**：登录、注册、帖子列表、帖子详情、帖子类别列表。
- **业务规则**：Token 缺失、无效或过期时返回 401，前端自动跳转登录页。

---

### 3.2 帖子（失物/招领）管理模块

#### FR-2.1 发布帖子（P0）

- **功能描述**：登录用户发布一条失物或招领信息。
- **输入**：
  | 字段 | 必填 | 说明 |
  |------|------|------|
  | type | 是 | LOST 失物 / FOUND 招领 |
  | title | 是 | 物品名称（≤50 字） |
  | category | 是 | 物品类别（13 类之一） |
  | location | 是 | 丢失/拾获地点 |
  | lostTime | 是 | 丢失/拾获时间 |
  | description | 否 | 详细描述（≤500 字） |
  | imageUrls | 否 | 图片相对路径，逗号分隔（最多 3 张） |
  | aiCategory | 否 | AI 自动识别的类别 |
- **处理逻辑**：
  1. 校验 type/title/category/location 非空、type 取值合法；
  2. 保存帖子，初始状态为"展示中"；
  3. 若含图片，调用 AI 服务提取多图特征向量（mean-pooling + L2 归一化）回填；
  4. 发布后自动执行智能匹配，命中阈值则生成匹配记录并通知双方。
- **业务规则**：AI 服务不可用时跳过特征提取，不影响发布，自动降级为纯文字匹配。
- **输出**：完整帖子对象（含 id）。

#### FR-2.2 帖子列表（P0）

- **功能描述**：分页浏览信息广场，支持多条件筛选。
- **筛选条件**：类型（全部/失物/招领）、关键词（物品名称模糊）、类别、地点、状态。
- **业务规则**：
  1. 默认只展示"展示中"状态的帖子；
  2. 按发布时间倒序排列；
  3. 每条帖子附带发布者昵称、头像。
- **输出**：分页结果（记录列表 + 总条数）。

#### FR-2.3 帖子详情（P0）

- **功能描述**：查看某条帖子的完整信息（含图片、描述、发布者）。
- **输入**：帖子 id。
- **输出**：帖子完整信息 + 该帖的智能匹配结果列表（含匹配依据）。

#### FR-2.4 我的帖子（P0）

- **功能描述**：查看当前用户发布的全部帖子。
- **输出**：帖子列表，按发布时间倒序。

#### FR-2.5 编辑帖子（P0）

- **功能描述**：作者修改自己发布的帖子内容。
- **业务规则**：
  1. 仅作者本人可编辑；
  2. 帖子已下架（非"展示中"）时不可编辑；
  3. 保存后重新提取图像特征并重新执行智能匹配。
- **输出**：更新后的帖子对象。

#### FR-2.6 删除帖子（P0）

- **功能描述**：作者删除自己发布的帖子。
- **业务规则**：
  1. 仅作者本人可删除；
  2. 删除时同时清除该帖关联的匹配记录。

#### FR-2.7 归档/下架帖子（P1）

- **功能描述**：作者手动将帖子下架归档。
- **业务规则**：仅作者本人可操作；归档后帖子不再在广场展示。

#### FR-2.8 物品类别列表（P0）

- **功能描述**：返回系统支持的物品类别，供表单下拉选择。
- **输出**：13 个类别——手机、钱包/包、钥匙、证件/卡、书包/背包、书籍、衣物、水杯、眼镜、耳机、电脑/电子设备、雨伞、其他。

---

### 3.3 智能匹配模块

#### FR-3.1 匹配算法（P0）

- **功能描述**：对一条失物帖与一条招领帖计算综合匹配度。

**文字匹配分（权重 0.30/0.25/0.25/0.20）：**

| 维度 | 权重 | 计算方法 |
|------|------|---------|
| 名称相似度 | 0.30 | 编辑距离 + 包含关系加权 |
| 类别匹配 | 0.25 | 同义词归一化后三档：相同 1.0 / 包含 0.8 / 其他 0.2（空值 0.5） |
| 地点相似度 | 0.25 | 编辑距离 + 包含关系加权 |
| 时间相近度 | 0.20 | 2h 内 1.0 / 24h 内 0.85 / 72h 内 0.65 / 168h 内 0.40 / 其余 0.15 |

**图像相似度**：双方帖子均有特征向量时，计算 960 维向量余弦相似度（0~1）。

**综合分融合：**

```
综合分 = 0.6 × 图像相似度 + 0.4 × 文字匹配分   （双方有图像时）
综合分 = 文字匹配分                            （任一方无图像时，AI 降级）
```

- **业务规则**：
  1. 综合分 ≥ 阈值（默认 0.35，可配置）时生成匹配记录并通知双方；
  2. 类别匹配先经同义词表归一化（如"双肩包"→"书包/背包"）；
  3. 阈值、图像/文字权重均可在 `application.yml` 中配置。

#### FR-3.2 帖子匹配结果（P0）

- **功能描述**：查看某帖子的实时匹配结果，按综合分倒序展示，附匹配依据拆解。
- **输出**：最多 20 条匹配项，每项包含对方帖子、综合分、文字分、图像分，以及名称/类别/地点/时间各维度得分、匹配记录状态。

#### FR-3.3 我的匹配（P0）

- **功能描述**：查看当前用户相关的全部匹配记录（作为失物方或招领方）。

#### FR-3.4 重新匹配（P1）

- **功能描述**：针对某帖子手动重新执行一次匹配计算。

#### FR-3.5 匹配记录状态（P0）

- **状态流转**：`NEW`（新匹配）→ `CONTACTED`（已发起认领）→ `CLAIMED`（已认领）/ 或 `NEW`（认领被拒后回退）；另有 `IGNORED`（已忽略）。

---

### 3.4 认领闭环模块

#### FR-4.1 发起认领申请（P0）

- **功能描述**：失主针对某条匹配记录向拾主发起认领申请。
- **输入**：匹配记录 id、认领说明（可选，≤200 字）。
- **业务规则**：
  1. 仅失物帖发布者（失主）可发起认领；
  2. 同一匹配记录下，同一失主不得重复发起待处理的申请；
  3. 发起成功后匹配记录状态由 `NEW` 更新为 `CONTACTED`；
  4. 系统向拾主发送"收到认领申请"通知。
- **输出**：认领记录对象。

#### FR-4.2 确认认领（P0）

- **功能描述**：拾主确认认领，完成闭环。
- **业务规则**：
  1. 仅招领帖发布者（拾主）可确认；
  2. 认领记录状态更新为 `CONFIRMED`，匹配记录状态更新为 `CLAIMED`；
  3. **失物帖与招领帖自动下架**（状态更新为"已认领"）；
  4. 分别向失主、拾主发送认领成功/完成通知。
- **输出**：操作成功提示。

#### FR-4.3 拒绝认领（P0）

- **功能描述**：拾主拒绝认领申请。
- **业务规则**：
  1. 仅拾主可拒绝；
  2. 认领记录状态更新为 `REJECTED`，匹配记录状态回退为 `NEW`；
  3. 向失主发送"认领申请被拒绝"通知。

#### FR-4.4 我的认领记录（P0）

- **功能描述**：查看当前用户相关的全部认领记录，包含"我发起的"与"需我处理的"两类。

---

### 3.5 消息通知模块

#### FR-5.1 通知列表（P0）

- **功能描述**：分页查看当前用户的通知，支持"仅未读"筛选。
- **通知类型**：`MATCH`（匹配成功）、`CLAIM`（认领相关）、`SYSTEM`（系统通知）。

#### FR-5.2 未读数统计（P0）

- **功能描述**：返回当前用户未读通知数量，用于导航栏红点提示。

#### FR-5.3 标记已读（P0）

- **功能描述**：将单条通知或全部通知标记为已读。

#### FR-5.4 实时推送（P1）

- **功能描述**：通过 SSE（Server-Sent Events）长连接向在线用户实时推送新通知。
- **业务规则**：SSE 断线或不可用时，前端自动降级为 15 秒轮询兜底，消息不丢失（消息已落库）。

---

### 3.6 数据看板模块

#### FR-6.1 统计总览（P0）

- **功能描述**：展示核心指标卡片。
- **指标**：信息总数、失物信息数、招领信息数、已认领闭环数、注册用户数、匹配记录数、认领记录数。

#### FR-6.2 类别分布（P1）

- **功能描述**：以环形图分别展示失物、招领的类别分布。

#### FR-6.3 高频地点 TOP10（P1）

- **功能描述**：以条形图展示丢失/拾获地点出现频次最高的前 10 名（仅统计展示中的帖子）。

#### FR-6.4 发布趋势（P1）

- **功能描述**：以折线图展示近 6 个月失物/招领发布数量变化趋势。

#### FR-6.5 匹配成功率（P1）

- **功能描述**：以仪表盘展示"认领闭环率 = 已认领匹配数 / 总匹配数"。

- **权限说明**：数据看板仅对管理员开放（前端路由鉴权 `admin: true`，后端 `AdminInterceptor` 校验 ADMIN 角色）。

---

### 3.7 AI 图像能力模块

#### FR-7.1 AI 自动识别物品类别（P0）

- **功能描述**：上传图片后调用 AI 服务自动识别物品类别，回填表单类别字段。
- **输入**：图片文件。
- **处理逻辑**：MobileNetV3 分类头 top-5 预测 → ImageNet 英文标签 → 中文校园物品类别映射表（587 条）。
- **输出**：中文类别 + 置信度（0~1）。
- **降级**：AI 不可用时返回 `available=false`，提示用户手动选择类别。

#### FR-7.2 图像特征提取（P0）

- **功能描述**：发布/编辑帖子时，对图片提取 960 维特征向量并存储，用于图像相似度匹配。
- **处理逻辑**：多图分别提取后做 mean-pooling + L2 归一化；单图直接使用；网络图片跳过本地提取。

#### FR-7.3 AI 健康检查（P1）

- **功能描述**：返回 AI 服务当前是否可用，前端据此展示"在线/已降级"状态。

#### FR-7.4 降级熔断（P0）

- **功能描述**：AI 服务请求失败后进入 30 秒冷却期（可配置），冷却期内直接走纯文字匹配；冷却结束自动重试，成功即恢复图像匹配。核心功能全程零影响。

---

### 3.8 文件上传模块

#### FR-8.1 图片上传（P0）

- **功能描述**：上传图片文件并返回访问路径。
- **支持格式**：jpg、jpeg、png、gif、webp、bmp。
- **业务规则**：
  1. 扩展名白名单校验；
  2. **文件头魔数校验**（JPEG/PNG/GIF/BMP/WebP 文件头字节比对），防止伪造扩展名上传恶意文件；
  3. 文件以 UUID 重命名，按日期分目录存储；
  4. 文件大小上限 10MB（单文件）、50MB（单请求），可配置。
- **输出**：图片访问 URL（`/uploads/...`）与相对路径。

---

## 4. 非功能需求

### 4.1 性能

| 指标 | 要求 |
|------|------|
| 接口响应时间 | 常规查询 < 500ms |
| 图片上传 | 单张 < 3s |
| AI 推理 | 单张图片 CPU 推理 < 2s（可接受） |
| 并发 | 支持教学演示场景下多用户同时访问 |

### 4.2 安全

1. 密码 BCrypt 加密存储，接口不返回密码字段；
2. JWT 无状态鉴权，拦截器统一校验；
3. 上传文件扩展名 + 文件头魔数双重校验；
4. 数据库查询采用 MyBatis-Plus 参数化查询，防 SQL 注入；
5. 帖子增删改校验归属权限，防止越权操作；管理员可通过 `/api/admin/**` 管理接口治理全站帖子（下架/删除）。

### 4.3 可用性

1. AI 服务故障时自动降级为纯文字匹配，核心功能零影响；
2. SSE 断线自动降级为轮询，消息不丢失；
3. 前端未构建时后端返回友好引导页而非报错。

### 4.4 可维护性 / 可扩展性

1. 匹配阈值、权重、AI 地址、冷却时间等均为配置项，无需改代码即可调参；
2. 前后端分离，后端可独立部署并托管前端构建产物（单端口生产形态）；
3. 集成 Knife4j 接口文档，便于接口查阅与联调。

---

## 5. 数据字典

### 5.1 用户表（t_user）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| username | VARCHAR(50) | 用户名（唯一） |
| password | VARCHAR(100) | 密码（BCrypt） |
| nickname | VARCHAR(50) | 昵称 |
| role | VARCHAR(20) | 角色：USER / ADMIN |
| avatar | VARCHAR(255) | 头像 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(100) | 邮箱 |
| create_time | DATETIME | 注册时间 |

### 5.2 帖子表（t_post）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 发布者 ID |
| type | VARCHAR(10) | LOST 失物 / FOUND 招领 |
| title | VARCHAR(100) | 物品名称 |
| category | VARCHAR(50) | 物品类别 |
| location | VARCHAR(100) | 丢失/拾获地点 |
| lost_time | DATETIME | 丢失/拾获时间 |
| description | TEXT | 详细描述 |
| image_urls | VARCHAR(1000) | 图片相对路径（逗号分隔） |
| feature_vector | TEXT | AI 图像特征向量（960 维） |
| status | TINYINT | 0 展示中 / 1 已认领 / 2 已归档 |
| ai_category | VARCHAR(50) | AI 自动识别类别 |
| create_time / update_time | DATETIME | 发布/更新时间 |

### 5.3 匹配记录表（t_match_record）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| lost_post_id / found_post_id | BIGINT | 失物帖 / 招领帖 ID（联合唯一） |
| text_score | DOUBLE | 文字匹配分（0~1） |
| image_score | DOUBLE | 图像相似度（0~1，可空） |
| score | DOUBLE | 综合匹配分（0~1） |
| status | VARCHAR(20) | NEW / CONTACTED / CLAIMED / IGNORED |
| create_time | DATETIME | 匹配时间 |

### 5.4 认领记录表（t_claim_record）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| match_id | BIGINT | 匹配记录 ID |
| post_id | BIGINT | 被认领的失物帖 ID |
| claimant_id | BIGINT | 申请人（失主）ID |
| message | VARCHAR(500) | 认领说明 |
| status | VARCHAR(20) | PENDING / CONFIRMED / REJECTED |
| create_time | DATETIME | 申请时间 |

### 5.5 通知表（t_notification）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 接收用户 ID |
| type | VARCHAR(20) | MATCH / CLAIM / SYSTEM |
| title / content | VARCHAR | 标题 / 内容 |
| related_id | BIGINT | 关联的匹配记录或帖子 ID |
| is_read | TINYINT | 0 未读 / 1 已读 |
| create_time | DATETIME | 时间 |

---

## 6. 接口清单

### 6.1 认证（/api/auth）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | /login | 登录，返回 Token | 否 |
| POST | /register | 注册 | 否 |
| GET | /me | 当前用户信息 | 是 |

### 6.2 帖子（/api/post）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | / | 发布帖子 | 是 |
| GET | /list | 帖子分页列表（type/category/keyword/location/status/page/size） | 否 |
| GET | /detail/{id} | 帖子详情 | 否 |
| GET | /my | 我的帖子 | 是 |
| PUT | /{id} | 编辑帖子 | 是 |
| DELETE | /{id} | 删除帖子 | 是 |
| POST | /{id}/archive | 归档/下架帖子 | 是 |
| GET | /categories | 类别列表 | 否 |

### 6.3 匹配（/api/match）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | /post/{postId} | 帖子匹配结果（含依据） | 是 |
| GET | /my | 我的匹配 | 是 |
| POST | /run/{postId} | 重新匹配 | 是 |

### 6.4 认领（/api/claim）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | /apply | 发起认领 | 是 |
| GET | /my | 我的认领记录 | 是 |
| POST | /{id}/confirm | 确认认领（自动下架） | 是 |
| POST | /{id}/reject | 拒绝认领 | 是 |

### 6.5 通知（/api/notification）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | /list | 通知列表（page/size/unreadOnly） | 是 |
| GET | /unread-count | 未读数 | 是 |
| PUT | /read/{id} | 标记已读 | 是 |
| PUT | /read-all | 全部已读 | 是 |
| GET | /stream | SSE 实时推送 | 是 |

### 6.6 统计（/api/stats）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | /overview | 统计总览 | 管理员 |
| GET | /category | 类别分布 | 管理员 |
| GET | /location | 地点 TOP10 | 管理员 |
| GET | /trend | 近 6 个月趋势 | 管理员 |
| GET | /success-rate | 匹配成功率 | 管理员 |

### 6.7 AI（/api/ai）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | /classify | 图片 → 自动识别类别 | 是 |
| GET | /health | AI 服务健康检查 | 是 |

### 6.8 文件（/api/file）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | /upload | 图片上传 | 是 |

### 6.9 管理员（/api/admin）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | /posts | 分页查看全部帖子（含已下架/已归档，支持 type/status/keyword 筛选） | 管理员 |
| POST | /post/{id}/archive | 下架任意帖子 | 管理员 |
| DELETE | /post/{id} | 删除任意帖子（含清理关联的匹配/认领记录） | 管理员 |
| GET | /users | 用户列表（含每人发布帖子数） | 管理员 |

### 6.10 接口文档

- 启动后端后访问 `http://localhost:8080/doc.html` 查看 Knife4j 在线接口文档。

---

## 7. 附录：统一响应格式

```json
{
  "code": 200,       // 200 成功；401 未登录；500 业务错误
  "msg": "success",  // 提示信息
  "data": {}         // 业务数据
}
```

- 分页结果 `data` 结构：`{ "records": [], "total": 0, "page": 1, "size": 10 }`。
