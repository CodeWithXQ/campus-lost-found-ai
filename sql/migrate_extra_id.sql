-- =====================================================================
-- 迁移脚本：通知表新增 extra_id 字段
-- 用途：一键联系时记录"我的帖子ID"，用于详情页只保留与我的物品的匹配
-- 说明：若数据库尚未初始化，直接重新执行 init.sql 即可（已包含此字段）；
--       若已初始化，请执行本脚本。
-- =====================================================================
USE lost_found;

ALTER TABLE t_notification
    ADD COLUMN extra_id BIGINT DEFAULT NULL COMMENT '额外关联ID（一键联系时我的帖子ID）' AFTER related_id;
